var g: int = 10;

proc bump(ref v: int): void {
    v = v + 1;
}

proc main(): void {
    var a: [1..5] int;
    var p: c_ptr(int);
    var i: int = 1;

    while (i <= 5) {
        a[i] = i * 2;
        i = i + 1;
    }

    p = c_ptrTo(g);
    bump(g);
    writeInt(*p);
    writeInt(a[3]);
}
